- hosts: all
  gather_facts: no
  become: yes
  vars:
    ansible_python_interpreter: /usr/bin/python3

  tasks:

    - apt:
        name: python3-pip
        update_cache: yes
        cache_valid_time: 604800 # 1 week

    - pip:
        name: docker
